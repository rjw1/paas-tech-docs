<%
wrap_layout :core do
  html = yield

  content_for(:toc_module, "in-page-navigation")

  content_for :sidebar do
	  table_of_contents(
		  if not 'index.html'
	          current_resource.parent
		  else
	          current_resource
		  end
    )
  end

  html
end
%>
